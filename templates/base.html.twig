<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>{% block title %}Le coin de Clément{% endblock %}</title>
    <style>
    *{
      padding:0;
      margin:0;
    }
      body{
        padding-bottom: 5em;
        padding-top: 5em;
      }

    </style>
  </head>
  <header>
  {% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark d-flex fixed-top">
      <div class="mr-auto p-2">
        <a href="/home" class="text-decoration-none text-light navbar-brand">Le Coin de Clément</a>
        <a href="/add" class="text-decoration-none text-secondary navbar-brand nav-link">Déposer une annonce</a>
      </div>
      <div class="p-2">
       {% if app.user == null %}
        <a href="/login" class="text-decoration-none text-secondary navbar-brand nav-link">Se connecter</a>
        <a href="/register" class="text-decoration-none text-secondary navbar-brand nav-link">S'inscrire</a>
        {% endif %}
      {% if app.user %}
        <a href="#" class="text-decoration-none text-secondary navbar-brand nav-link">Bonjour {{app.user.email}}</a>
        <a href="{{ path('app_logout') }}" type="button" class="btn btn-danger">Logout</a>
        {% endif %}
        <a href="/home_en" class="text-decoration-none btn text-secondary navbar-brand nav-link">English</a>
        <a href="/home" class="text-decoration-none btn text-secondary navbar-brand nav-link">Français</a>
      </div>
    </nav>
  {% endblock %}
  </header>
  <body>
   {% block body %} {% endblock %}
  </body>

  <footer>
  {% block footer %} 
  <div class="bg-dark text-light card-footer fixed-bottom d-flex justify-content-center">
    <h3>Copyright @Clément_Seidllot</h3>
  </div>
  {% endblock %}
  </footer>
</html>